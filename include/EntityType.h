#pragma once

/// Abstract class that represents a type of game object
///
/// This is where information shared between components should be stored.
class EntityType
{
public:
	EntityType();
	~EntityType();
};

